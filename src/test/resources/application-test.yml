spring:
  profiles: test
  application:
    name: itemService

  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver


batchSize: 1000


logging:
  level:
    org:
      springframework:
        jdbc: debug

