spring:
  profiles: test
  application:
    name: itemService

  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver


batchSize: 1000


logging:
  level:
    org:
      springframework:
        jdbc: debug

